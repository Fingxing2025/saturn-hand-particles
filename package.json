{
  "name": "saturn-hand-particles",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "cert:lan": "node scripts/setup-lan-https.mjs",
    "dev:lan": "npm run cert:lan && VITE_LAN_HTTPS=1 vite --host 0.0.0.0 --port 4173",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.184.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "three": "^0.184.0"
  }
}
